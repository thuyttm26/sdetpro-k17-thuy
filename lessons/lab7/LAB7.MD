```js
// Data

userId = 1;
postId = 1;
// Constructor

const reqHanler = new RequestHandler(BASE_URL);
//Functionl Programming
const post = await getTargetPost(userId, postId);
const allPost = await getAllPost(userId);
```
